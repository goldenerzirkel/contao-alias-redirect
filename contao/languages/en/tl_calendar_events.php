<?php

declare(strict_types=1);

$GLOBALS['TL_LANG']['tl_calendar_events']['gozi_noRedirect'] = ['Do not create a redirect', 'When the alias changes, the old alias is normally added to the redirect list. Skip it once if the old alias should deliberately go nowhere.'];
$GLOBALS['TL_LANG']['tl_calendar_events']['gozi_redirects'] = ['Redirects to this page', 'Old aliases that lead here via 301 — one alias per line, without a leading slash. On rename the old alias is added automatically; every new alias becomes the target of all old ones. Changes are kept in the page versioning.'];
